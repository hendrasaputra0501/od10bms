<?xml version="1.0" ?>
<odoo>
    <data noupdate="1">
        <!--Email template -->
        <record id="po_approver_notification_email_template" model="mail.template">
            <field name="name">PO Approver Approval Notification - Send by Email</field>
            <field name="model_id" ref="bms_approval_notification.model_purchase_order"/>
            <field name="email_from">admin@bumimajusawit.co.id</field>
            <field name="email_to">${ctx.get('to')}</field>
            <field name="subject">Approval Notification - Purchase Order</field>
            <field name="body_html"><![CDATA[
                <p>PO berikut membutuhkan persetujuan anda untuk diproses lebih lanjut:</p>
                
                <ol>
                % for doc in ctx.get('docs'):
                    % set access_url = '/web#id=' + str(doc.id) + '&view_type=form&model=purchase.order'
                    <li><strong>${doc.name}</strong> Rp. ${'{0:,.2f}'.format(doc.amount_total)} Klik link berikut untuk melanjutkan proses : <a href="${access_url}">Purchase Order ${doc.name or 'n/a'}</a></li> 
                % endfor
                </ol>
                ]]></field>
        </record>
    </data>
</odoo>
